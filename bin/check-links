#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/../lib/init-node.sh"

if [[ $# -gt 0 ]]; then
    exec npx markdown-link-check --retry --verbose -- "$@"
fi

cd "$(dirname "$0")/.."
exec npx markdown-link-check --retry --verbose -- *.md
