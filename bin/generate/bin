#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../.."

default_version='1.2.3-dev'
export BIN_VERSION=${1-$default_version}

if [[ $BIN_VERSION = '--help' || $BIN_VERSION = '-h' ]]; then
    echo "Usage: ${BIN_COMMAND-$0} [version=$default_version]"
    exit
fi

cargo test
cargo build
